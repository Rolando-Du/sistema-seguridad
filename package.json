{
  "name": "sistema-seguridad-nacional",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema fullstack de gestión, análisis, auditoría y reportes de incidentes de seguridad",
  "scripts": {
    "dev": "pnpm --parallel --filter client --filter server dev",
    "client:dev": "pnpm --filter client dev",
    "server:dev": "pnpm --filter server dev",
    "client:build": "pnpm --filter client build",
    "server:start": "pnpm --filter server start",
    "server:prisma:generate": "pnpm --filter server prisma:generate",
    "server:migrate:deploy": "pnpm --filter server prisma:migrate:deploy",
    "server:seed": "pnpm --filter server seed"
  },
  "keywords": [
    "react",
    "vite",
    "tailwindcss",
    "node",
    "express",
    "postgresql",
    "prisma",
    "seguridad",
    "incidentes"
  ],
  "author": "Rolando Duarte",
  "license": "ISC"
}
